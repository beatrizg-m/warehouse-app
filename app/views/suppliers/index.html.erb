<h1>Fornecedores</h1>

<% @suppliers.each do | s |%>
  <div>
    <dl>
      <dt><strong>Fornecedor:</strong></dt>
      <%= link_to(s.brand_name, supplier_path(s.id)) %>
      <dt><strong>Número de registro: </strong></dt>
      <dd><%= s.registration_number %></dd>
    </dl>
  </div>
<% end %>

<div>
  <%= link_to('Cadastrar Fornecedor', new_supplier_path)%>
  <%= link_to('Voltar', root_path)%>
</div>
